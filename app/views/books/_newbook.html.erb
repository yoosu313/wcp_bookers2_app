<!--@book_new → book_new-->
<h2>New Book</h2>
<%= form_with model: book_new, local: true do |f| %>
  <% if book_new.errors.any? %>
    <%= book_new.errors.count %> error
    <% book_new.errors.full_messages.each do |msg| %>
      <ul><li><%= msg %></li></ul>
    <% end %>
  <% end %>
  <div class="field">
    <%= f.label :title, "Title" %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body, "Body" %>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit 'Create Book' %>
  </div>
<% end %>